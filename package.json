{
  "name": "notecode",
  "version": "0.1.0-beta.1",
  "description": "NoteCode - AI Task Management with Electron Desktop Support",
  "author": "NoteCode Team",
  "license": "BSL-1.1",
  "main": "electron/dist/main.js",
  "type": "commonjs",
  "bin": {
    "notecode": "./backend/bin/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "backend/bin/**",
    "backend/dist/**",
    "backend/package.json",
    "backend/public/**",
    "frontend/dist/**"
  ],
  "scripts": {
    "electron:install": "cd electron && npm install",
    "electron:rebuild": "cd electron && npm run rebuild",
    "electron:build": "cd electron && npm run build",
    "electron:dev": "npm run build --prefix backend && concurrently \"npm run dev --prefix frontend\" \"wait-on http://localhost:5173 && cd electron && npm run dev\"",
    "electron:start": "npm run build --prefix backend && cd electron && npm run dev",
    "electron:package": "npm run build && cd electron && npm run build && npm run package",
    "electron:package:win": "npm run build && cd electron && npm run build && npm run package:win",
    "electron:package:mac": "npm run build && cd electron && npm run build && npm run package:mac",
    "electron:package:linux": "npm run build && cd electron && npm run build && npm run package:linux",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^33.4.11",
    "wait-on": "^7.2.0"
  },
  "workspaces": [
    "backend",
    "frontend",
    "electron"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "overrides": {
    "@electron/rebuild": {
      "node-gyp": "^12.0.0"
    }
  },
  "dependencies": {}
}
